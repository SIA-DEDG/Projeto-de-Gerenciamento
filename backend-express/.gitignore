node_modules
# Build output (gerado por `npm run build`, não versionar)
dist

# Keep environment variables out of version control
.env

/generated/prisma

# Guia interno de migrations (não versionar)
prisma/MIGRATIONS.md
